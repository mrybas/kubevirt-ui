apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: validating-webhook-configuration
  annotations:
    # Namespace spelled out because this overlay is what chose it.
    cert-manager.io/inject-ca-from: kubevirt-ui-operator-dev/kubevirt-ui-operator-serving-cert
